__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/
*.log
*.out
*.err

# experiment output -- generated by the runs on the server, not checked in
# by default (the directory structure itself is kept via .gitkeep so
# scripts have somewhere to write, but the CSV/JSON contents are not)
results/*
!results/.gitkeep
